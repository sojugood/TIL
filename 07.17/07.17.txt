변수
- 어떠한 "한 값"을 가진 메모리 공간에 붙인 "이름" 또는 공간 "그 자체"

변수명 본인의 정보를 작성해보세요
이름 name = "Sang Ik Sim"
나이 age = 26

one = 1 -> ox 0000000 -> 1
 변수     -> 메모리 주소 -> 값

id() # 값의 메모리 주소를 알려주는 함수

print(id(1))

one =1

print(id(one))

three = 1 + 2

print(id(2))

print(id(three))