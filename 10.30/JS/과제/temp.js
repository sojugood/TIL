/**
 * 이름, 나이 입력하면 합쳐서 문자로 반환하는 함수
 * @param {string} name 이름 넣으셈 
 * @param {number} age 나이 넣으셈
 * @returns 두 개 합쳐서 문자로 반환해줌
 * @version 1.25
 */
function hello(name, age) {
    return name + age
}

hello(kim, 25)